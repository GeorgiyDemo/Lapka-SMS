<p align="center">
  <img src="assets/logo.png" width="150" alt="Lapka SMS">
</p>

<h1 align="center">Lapka SMS</h1>

<p align="center">
  <b>پیام‌رسان SMS رمزنگاری‌شده برای اندروید</b><br>
  رمزنگاری SMS از طریق پنهان‌نگاری — پیام‌های شما مانند متن عادی به نظر می‌رسند
</p>

<p align="center">
  <a href="https://github.com/GeorgiyDemo/Lapka-SMS/actions"><img src="https://github.com/GeorgiyDemo/Lapka-SMS/actions/workflows/android.yml/badge.svg" alt="Build"></a>
  <a href="https://github.com/GeorgiyDemo/Lapka-SMS/releases"><img src="https://img.shields.io/github/v/release/GeorgiyDemo/Lapka-SMS?label=release" alt="Release"></a>
  <a href="LICENSE"><img src="https://img.shields.io/badge/license-GPLv3-blue" alt="License"></a>
  <img src="https://img.shields.io/badge/Android-6.0%2B-green" alt="Android 6.0+">
</p>

<p align="center">
  <a href="README.md">English version</a> · <a href="README_RU.md">Русская версия</a> · <a href="PROTOCOL_FA.md">پروتکل</a>
</p>

---

## این چیست؟

Lapka SMS یک اپلیکیشن کامل SMS با رمزنگاری داخلی پیام‌ها است. پیام‌های رمزنگاری‌شده با استفاده از پنهان‌نگاری کدگذاری می‌شوند — برای هر کسی که آنها را رهگیری کند، مانند متن عادی انگلیسی یا روسی به نظر می‌رسند. دشمن اصلی **اپراتور تلفن همراه** است که می‌تواند محتوای SMS را در حین انتقال بخواند.

> **هر دو طرف به Lapka SMS** با کلید رمزنگاری یکسان نیاز دارند تا پیام‌های رمزنگاری‌شده رد و بدل کنند. SMS معمولی با هر تلفنی کار می‌کند.

## امکانات

### رمزنگاری ([جزئیات پروتکل](PROTOCOL_FA.md))
- **AES-256-GCM** — رمزنگاری احراز هویت‌شده با استخراج کلید HKDF-SHA256
- **محافظت از بازپخش** — پیام‌های قدیمی و تکراری رد می‌شوند
- **پنهان‌سازی طول پیام** — padding از تحلیل ترافیک جلوگیری می‌کند

### طرح‌های پنهان‌نگاری
| طرح | نمونه خروجی | مناسب برای |
|---|---|---|
| Base64 | `dGVzdA==` | عمومی، فشرده |
| Base64 سیریلیک | `дГВздА==` | ترکیب با متن سیریلیک |
| کلمات روسی | `молоко дерево книга` | شبیه متن روسی |
| کلمات انگلیسی | `Drawcut Foussa Miranda` | شبیه متن انگلیسی |

### مدیریت کلید
- **کلید برای هر مکالمه** — کلید متفاوت برای هر مخاطب
- **کلید سراسری** — کلید پیش‌فرض برای همه مکالمات
- **اشتراک‌گذاری QR کد** — اسکن برای تبادل کلید
- **اثر انگشت SHA-256** — تأیید اصالت کلید
- **Android Keystore** — ذخیره‌سازی کلید با پشتیبانی سخت‌افزاری
- **EncryptedSharedPreferences** — کلیدها در حالت استراحت رمزنگاری شده‌اند

### حریم خصوصی و امنیت
- **FLAG_SECURE** — مخفی‌سازی محتوای برنامه در تعویض‌کننده وظایف (قابل تنظیم)
- **حذف خودکار پیام‌های رمزنگاری‌شده** — تایمر قابل تنظیم
- **SMS برای بازنشانی** — دریافت یک SMS از پیش تعیین‌شده همه کلیدهای رمزنگاری را پاک و تنظیمات رمزنگاری را بازنشانی می‌کند. پیام‌ها باقی می‌مانند، اما غیرقابل رمزگشایی می‌شوند
- **بدون تحلیل و ردیابی**
- **پایگاه داده رمزنگاری‌شده Realm**

### اپلیکیشن SMS
- رابط کاربری Material Design با تم‌های قابل تنظیم
- حالت شب (خودکار/دستی/سیستم)
- تنظیمات اعلان برای هر مکالمه
- ارسال با تأخیر
- گزارش تحویل
- پشتیبانی از دو سیم‌کارت
- عملیات کشیدن
- ۳۸ زبان

## مدل تهدید

| محافظت‌شده | محافظت‌نشده |
|---|---|
| محتوای پیام (AES-256-GCM) | فراداده‌های ارتباطی (چه کسی، چه زمانی، چند بار) |
| طول پیام (padding) | استفاده هر دو طرف از Lapka SMS |
| مواد کلید در ذخیره‌سازی (Keystore + EncryptedSharedPreferences) | دسترسی فیزیکی به دستگاه باز شده |
| محتوای برنامه در تعویض‌کننده وظایف (FLAG_SECURE) | امنیت دستگاه گیرنده |

## شروع کار

### ۱. نصب

آخرین APK را از [Releases](https://github.com/GeorgiyDemo/Lapka-SMS/releases) دانلود و نصب کنید. هنگام درخواست، به عنوان اپلیکیشن پیش‌فرض SMS تنظیم کنید.

> **Google Play Protect ممکن است نصب را مسدود کند** زیرا برنامه از طریق Google Play توزیع نمی‌شود. این یک مثبت کاذب است — برنامه متن‌باز است و بدافزار ندارد. برای نصب:
>
> ۱. اگر **«برنامه برای محافظت از دستگاه شما مسدود شد»** را می‌بینید — باید Play Protect را موقتاً غیرفعال کنید:
>    - **Google Play** را باز کنید ← روی **آیکون پروفایل** ضربه بزنید ← **Play Protect** ← **تنظیمات** (آیکون چرخ‌دنده) ← **«اسکن برنامه‌ها با Play Protect»** را خاموش کنید
>    - APK را نصب کنید
>    - پس از نصب، Play Protect را دوباره فعال کنید
> ۲. اگر پس از نصب **«درخواست استفاده به عنوان برنامه پیش‌فرض SMS رد شد»** را می‌بینید — اندروید ۱۳ به بعد دسترسی برنامه‌های نصب‌شده از خارج فروشگاه را محدود می‌کند. برای رفع:
>    - به **تنظیمات اندروید** ← **برنامه‌ها** ← **Lapka SMS** را پیدا کنید ← روی **⋮** (سه نقطه، بالا سمت راست) ← **«اجازه تنظیمات محدود»** ضربه بزنید
>    - حالا Lapka SMS را باز کنید و درخواست برنامه پیش‌فرض SMS را بپذیرید

### ۲. راه‌اندازی رمزنگاری

یک مکالمه را باز کنید ← روی منوی **⋮** ← **جزئیات** ← **کلید رمزنگاری** ضربه بزنید.

۱. کلید **رمزنگاری** را فعال کنید
۲. روی **تولید کلید جدید** ضربه بزنید — یک کلید تصادفی AES-256 ایجاد می‌شود
۳. کلید را با مخاطب خود از طریق **QR کد** (حضوری) یا کانال امن به اشتراک بگذارید. **هرگز کلید را از طریق SMS معمولی ارسال نکنید!**
۴. مطمئن شوید که **اثر انگشت اموجی** در هر دو دستگاه مطابقت دارد
۵. به پایین بروید و **طرح کدگذاری** را انتخاب کنید

<img src="assets/guide/generate_key.png" width="300">

<img src="assets/guide/encoding.png" width="300">

طرح‌های کدگذاری موجود:

| طرح | شبیه |
|---|---|
| Base64 | `dGVzdA==` — نویسه‌های تصادفی |
| Base64 سیریلیک | `дГВздА==` — نویسه‌های سیریلیک |
| کلمات روسی | `молоко дерево книга` — متن روسی |
| کلمات انگلیسی | `Drawcut Foussa Miranda` — متن انگلیسی |

### ۳. ارسال پیام‌های رمزنگاری‌شده

**آیکون قفل** 🔒 کنار هر پیام نشان‌دهنده فعال بودن رمزنگاری است. فقط بنویسید و ارسال کنید — پیام‌ها به طور خودکار رمزنگاری می‌شوند.

<p float="left">
  <img src="assets/guide/encryption.png" width="300">
  <img src="assets/guide/default_messenger.png" width="300">
</p>

*چپ: Lapka SMS (نمای رمزگشایی‌شده) · راست: نمای پیام در اپلیکیشن SMS معمولی*

## ساخت از سورس

نیاز به **JDK 17**.

```bash
git clone https://github.com/GeorgiyDemo/Lapka-SMS.git
cd Lapka-SMS
./gradlew assembleDebug
```

## معماری

```
presentation/   لایه رابط کاربری اندروید (Activities, Conductor Controllers, ViewModels)
domain/          منطق کسب‌وکار، تعامل‌کننده‌ها، مدل‌ها
data/            مخازن، دریافت‌کننده‌ها، پایداری Realm
common/          ابزارهای مشترک
psms-lib/        کتابخانه رمزنگاری (AES-GCM, HKDF, کدگذاری‌کننده‌های پنهان‌نگاری)
android-smsmms/  فریمورک قدیمی MMS/SMS
```

الگوهای کلیدی:
- **Conductor** — ناوبری (Controllers داخل Activities)
- **Dagger 2** — تزریق وابستگی
- **RxJava 2** + AutoDispose — جریان‌های واکنشی
- **Realm** — ذخیره‌سازی رمزنگاری‌شده داده‌ها

## تفاوت با upstream

Lapka SMS یک فورک از [Partisan-SMS](https://github.com/wrwrabbit/Partisan-SMS) (که خود فورکی از [QKSMS](https://github.com/moezbhatti/qksms) است). تغییرات در Lapka SMS:

- ارتقای پروتکل رمزنگاری به نسخه ۳ با کدگذاری‌کننده‌های پنهان‌نگاری جدید
- ارتقای وابستگی‌ها (Dagger 2.52, Glide 4.16, Kotlin 1.9, compileSdk 35)
- ذخیره‌سازی رمزنگاری‌شده پایگاه داده (رمزنگاری Realm از طریق Android Keystore)
- EncryptedSharedPreferences برای مواد کلید
- تأیید اثر انگشت کلید
- انتخاب زبان داخل برنامه
- تقویت امنیت (تنظیمات امنیت شبکه، لاگ‌گذاری خصوصی، FLAG_SECURE)
- خط لوله CI/CD
- مدرن‌سازی کد (پاکسازی API منسوخ، مهاجرت به AndroidX)

## مجوز

[GNU General Public License v3.0](LICENSE)
